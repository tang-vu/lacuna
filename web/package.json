{
  "name": "lacuna-web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --test scripts/*.test.mjs",
    "build": "npm test && tsc --noEmit && vite build && node scripts/build-share-pages.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
